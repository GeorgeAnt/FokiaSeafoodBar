---
import { t, defaultLocale, type Locale } from '../lib/i18n';

interface Props {
  locale?: Locale;
}
const { locale = defaultLocale } = Astro.props;
---

{/*
  The mid tier, not dark. The hero is already --black, so a dark Goal ran
  straight into it with no boundary and made the top of the page one mass. Stone
  suits a text-only section: there are no photographs here to sit against it.
*/}
<section class="section section-stone" id="goal">
  {/* One reveal for the whole band: the eyebrow, the lead and the two columns
      are a single thought, and staggering them made the eyebrow arrive on its
      own above nothing. */}
  <div class="wrap" data-reveal>
    <p class="eyebrow" data-i18n="nav.goal">{t(locale, 'nav.goal')}</p>
    <h2 class="goal__lead" data-i18n="goal.lead">{t(locale, 'goal.lead')}</h2>

    <div class="goal__grid">
      <p data-i18n="goal.body">{t(locale, 'goal.body')}</p>
      <p data-i18n="goal.body2">{t(locale, 'goal.body2')}</p>
    </div>
  </div>
</section>
